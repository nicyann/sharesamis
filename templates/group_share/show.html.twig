{% extends 'base.html.twig' %}

{% block title %}GroupShare{% endblock %}

{% block body %}
    <h1>GroupShare</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_share.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ group_share.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ group_share.description }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ group_share.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_share_index') }}">back to list</a>

    <a href="{{ path('group_share_edit', {'id': group_share.id}) }}">edit</a>

    {{ include('group_share/_delete_form.html.twig') }}
{% endblock %}
